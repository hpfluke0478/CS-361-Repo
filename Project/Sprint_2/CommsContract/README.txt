Communication Contract

Requesting and Recieving Data: Stored users can be accessed within data.db file with the aid of SQLiteViewer in VS Code

Note: Data requests are also achieved during runtime when the user input is verified against accounts present in the database.
      Moreoever, data receival operations are achieved when said user input is transmitted from the client to the server.

UML Sequence Diagram: located under the CommsContract directory of this GitHub.